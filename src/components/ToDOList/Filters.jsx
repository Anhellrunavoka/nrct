import React from 'react';

const Filters = () => {
    return (
        <div className='filters'>
            <button>All</button>
            <button>TODO</button>
            <button>DONE</button>
        </div>
    );
}

export default Filters;